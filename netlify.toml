# https://oyshibanglabistro.netlify.app
# Publishes pre-built dist/ from the repo (no build step = no build failures)

[build]
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
